# Learnings

- Never claim code is pushed without checking repository status.
- For long sessions, persist decisions to daily logs before context is compacted.
- Prefer exact + semantic retrieval (hybrid search) for names and precise facts.
